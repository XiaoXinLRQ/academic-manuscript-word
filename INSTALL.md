# 交给 Codex 完成安装

用户无需运行脚本或手动复制文件。在 Codex 中发送 README 的快速安装指令即可。本文供执行安装的 Codex 阅读；只有用户明确请求安装或设置默认格式时才执行，不因阅读本文件而修改用户环境。

## 安装流程

1. 解析用户的 Codex 目录：优先 CODEX_HOME，否则使用用户主目录下的 `.codex`。不要依赖发布者的绝对路径。
2. 使用可用的 skill-installer，从 `XiaoXinLRQ/academic-manuscript-word` 仓库安装 `academic-manuscript-word` 子目录。若没有该工具，下载仓库并只复制该子目录至个人 `skills/academic-manuscript-word`。不要求用户安装 GitHub 插件，公开仓库可直接下载。
3. 如果目标目录已存在，先比较内容。相同则跳过；不同且用户请求更新，则备份后更新；仅请求安装时保留本地版本，说明已有安装，不直接覆盖用户改动。不重复下载已确认一致的文件。
4. 若用户同时授权设为默认格式，读取已有全局 `AGENTS.md`，检查是否已有指向本 Skill 的默认 Word 规则。已有等效规则则保留；没有则追加下面的标记块。若同名块已存在则仅更新块内内容，保留块外全部指令；有未标记的冲突默认规则时说明冲突，不盲目叠加。写入前备份已有文件，不替换整个文件。若存在优先加载的全局 `AGENTS.override.md`，核实实际加载路径并在该文件中合并规则，不把规则写入不会被加载的文件。
5. 标记块中的 Skill 路径应替换为当前用户实际安装文件的绝对路径。保留用户当前任务指定模板优先的规则。
6. 验证 Skill 文件、配置 JSON 和 UI 元数据存在且可读；有校验工具时运行一次。确认默认指令只出现一次，然后说明安装结果及生效所需的新任务/重新加载。不要为了安装验证生成 Word、导出 PDF、安装字体或安装整套文档工具；文档生成环境在首次生成时按需检查。

## 默认指令标记块

```markdown
<!-- academic-manuscript-word:begin -->
生成新的 Word/DOCX 文件时，默认读取并应用 <实际安装路径>/academic-manuscript-word/SKILL.md 及其集中配置，无需用户显式调用。使用宋体/SimSun、Times New Roman、A4 和简洁的原生 Word Styles。写入内容前清除标题主题字体、主题颜色与装饰边框；完成 PDF 逐页视觉检查，首轮通过即结束，仅有实际问题或文件修改时重新导出检查。按内容选择样式，不强制添加论文章节；内容生成按任务授权执行，排版保护已确定内容。用户指定其他模板或版式时优先遵循。此默认规则不要求修改已有 Word。
<!-- academic-manuscript-word:end -->
```

## 安装后使用

直接要求“生成一份 Word 文件”即可应用默认版式。仅安装 Skill、未设置默认指令时，可显式使用 `$academic-manuscript-word`。用户明确要求其他版式时以该任务要求为准。
