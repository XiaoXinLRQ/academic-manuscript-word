# 默认 Word 生成格式

当用户要求新建或生成 Word/DOCX 文件时，默认读取个人 skills 目录中的 academic-manuscript-word/SKILL.md 及其集中配置，无需用户显式调用。采用宋体/SimSun 中文、Times New Roman 英文、A4、原生 Word Styles 和简洁传统的学术稿件版式。

在写入内容前清除 Title、Heading 1–4 的主题字体、主题颜色及装饰边框，首次导出前检查有效样式。完成 DOCX→PDF→逐页视觉检查，检查通过即结束，仅在发现实际问题或文件修改后重新导出检查。

此规则是新建 Word 的默认格式，不是修改已有 Word 的指令。按任务内容选择样式，不强制添加摘要、关键词、参考文献或论文章节。内容生成按用户授权执行，排版阶段保护已确定内容。用户明确指定其他模板时以其要求为准。其他文档工具也遵循该配置，不用通用设计预设覆盖它。
